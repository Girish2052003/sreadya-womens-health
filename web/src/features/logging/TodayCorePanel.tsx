import type { HealthObservation } from '../../domain/cycle/types';

export function TodayCorePanel({ observations, today }: { observations: HealthObservation[]; today: string }) {
  void observations;
  void today;
  return <div>Task 10 Today panel not implemented.</div>;
}
