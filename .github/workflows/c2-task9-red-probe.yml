name: C2 Task 9 RED Probe

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  cycle-domain-red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '24.21.0'
          cache: npm
          cache-dependency-path: web/package-lock.json
      - name: Install locked dependencies
        working-directory: web
        run: npm ci --ignore-scripts
      - name: Strict TypeScript
        working-directory: web
        run: npm run typecheck
      - name: Run Task 9 focused tests
        working-directory: web
        run: npm test -- --run src/domain/cycle/invariants.test.ts src/domain/cycle/types.test.ts src/domain/cycle/schema-conformance.test.ts src/vault/health-repository.test.ts
